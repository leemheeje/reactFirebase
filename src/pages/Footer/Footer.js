import React from 'react';
import styles from './Footer.module.scss';

const Header = ({ }) => {
	return (
		<div className={styles.container}>
			<div className={styles.innerWrap}> sdf </div>
		</div>
	)
}
export default Header;